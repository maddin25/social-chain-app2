<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
	xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
	xmlns:flv="clr-namespace:DLToolkit.Forms.Controls;assembly=DLToolkit.Forms.Controls.FlowListView"
	x:Class="PartyTimeline.EventPageThumbnails"
	>
	<ContentPage.Content>
		<StackLayout Orientation="Vertical">
			<Label x:Name="EventNameLabel" Text="No Eventname Assigned" XAlign="Center" FontSize="30"/>
			<flv:FlowListView
				x:Name="EventImageList"
				SeparatorVisibility="None"
				HasUnevenRows="true"
				FlowColumnMinWidth="150">
			
				<flv:FlowListView.FlowColumnTemplate>
					<DataTemplate>
						<Grid Padding="3">
							
							<Grid.RowDefinitions>
								<RowDefinition Height="*" />
							</Grid.RowDefinitions>
							<Grid.ColumnDefinitions>
								<ColumnDefinition Width="*" />
							</Grid.ColumnDefinitions>
							
							<Image Source="{Binding URI}" HeightRequest="150" WidthRequest="150" Aspect="AspectFill"/>

							<Label x:Name="Label" HorizontalOptions="Fill" HorizontalTextAlignment="Center" VerticalOptions="End"
								BackgroundColor="Silver" Opacity="0.8" Text="{Binding ShortAnnotation}"/>
						</Grid>
					</DataTemplate>
				</flv:FlowListView.FlowColumnTemplate>
			</flv:FlowListView>
			<Button Text="Take Photo" />
		</StackLayout>
	</ContentPage.Content>
</ContentPage>
