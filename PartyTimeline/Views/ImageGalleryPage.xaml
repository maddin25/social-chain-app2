<?xml version="1.0" encoding="utf-8" ?>
<CarouselPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PartyTimeline.Views.ImageGalleryPage"
             Title="Event Images"
             ItemsSource="{Binding Images}"
	>
    <CarouselPage.ItemTemplate>
        <DataTemplate>
            <ContentPage>
                <ContentPage.Padding>
                    <OnPlatform x:TypeArguments="Thickness" iOS="0,40,0,0" Android="0,40,0,0" />
                </ContentPage.Padding>
                <StackLayout>
                    <Image Source="{Binding URI}" HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand" />
                    <Label Text="{Binding ShortAnnotation}" FontSize="Medium" HorizontalOptions="Center" />
                </StackLayout>
            </ContentPage>
        </DataTemplate>
    </CarouselPage.ItemTemplate>
</CarouselPage>