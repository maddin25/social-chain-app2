@model IEnumerable<PartyTimeLineWebServices.Models.EventImage>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DateTaken)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ShortAnnotation)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LongAnnotation)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.URI)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DateTaken)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ShortAnnotation)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LongAnnotation)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.URI)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
