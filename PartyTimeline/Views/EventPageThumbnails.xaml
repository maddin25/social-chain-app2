<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
	xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
	x:Class="PartyTimeline.EventPageThumbnails"
	xmlns:ffimageloading="clr-namespace:FFImageLoading.Forms;assembly=FFImageLoading.Forms"
	>

    <ContentPage.Content>
		<StackLayout Orientation="Vertical">
			<ListView
				x:Name="ListViewEventThumbnails"
				RowHeight="200"
				ItemsSource="{Binding EventReference.Images}"
				SelectedItem="{Binding SelectedItem, Mode=TwoWay}"
				IsPullToRefreshEnabled="true"
				RefreshCommand="{Binding RefreshListCommand}"
				>

				<ListView.Header>
					<Label Text="{Binding EventReference.Name}" XAlign="Center" FontSize="30"/>
				</ListView.Header>
				
				<ListView.ItemTemplate>
					<DataTemplate>
						<ViewCell>
							<AbsoluteLayout Padding="0,0,0,6" >
								<ffimageloading:CachedImage
									AbsoluteLayout.LayoutBounds="0, 0, 1, 1"
									AbsoluteLayout.LayoutFlags="All"
									HeightRequest="200"
									Aspect="AspectFill"
									DownsampleHeight="200"
									DownsampleUseDipUnits="false"
									LoadingPlaceholder="image_loading.png"
									ErrorPlaceholder="image_error.png"
									Source="{Binding URI}">
	                            </ffimageloading:CachedImage>
								
								<Label
									AbsoluteLayout.LayoutBounds="0.5, 1, 1, 0.3"
									AbsoluteLayout.LayoutFlags="All"
									VerticalOptions="End"
									HorizontalTextAlignment="Center"
									VerticalTextAlignment="End"
									BackgroundColor="Silver"
									Opacity="0.8"
									Text="{Binding Caption}"
									/>
									
							</AbsoluteLayout>
                         </ViewCell>
					</DataTemplate>
				</ListView.ItemTemplate>
				
			</ListView>
			
			<!--<flv:FlowListView
				SeparatorVisibility="None"
				HasUnevenRows="true"
				FlowColumnMinWidth="150"
				ItemsSource="{Binding EventReference.Images}"
				>
				
				<flv:FlowListView.FlowColumnTemplate>
					<DataTemplate>
                        <Grid Padding="3">
                            <Grid.RowDefinitions>
								<RowDefinition Height="*" />
							</Grid.RowDefinitions>
							<Grid.ColumnDefinitions>
								<ColumnDefinition Width="*" />
							</Grid.ColumnDefinitions>

                            <ffimageloading:CachedImage
								HeightRequest="150"
								Aspect="AspectFill"
								DownsampleHeight="150"
								DownsampleUseDipUnits="false"
								LoadingPlaceholder="image_loading.png"
								ErrorPlaceholder="image_error.png"
								Source="{Binding URI}">
                            </ffimageloading:CachedImage>
							
                            <Label HorizontalOptions="Fill" HorizontalTextAlignment="Center" VerticalOptions="End"
								BackgroundColor="Silver" Opacity="0.8" Text="{Binding ShortAnnotation}"/>
						</Grid>
					</DataTemplate>
				</flv:FlowListView.FlowColumnTemplate>
			</flv:FlowListView>-->
			
			<Button Text="Take Photo" Command="{Binding TakePhotoButtonCommand}" HeightRequest="40" />
            <Button Text="Pick Photo" Command="{Binding PickPhotoButtonCommand}" HeightRequest="40" />
          </StackLayout>
	</ContentPage.Content>
</ContentPage>
